# Server-only (Cloudflare Worker secret / local wrangler .dev.vars)
# Neon PostgreSQL connection string. NEVER expose this to clients.
DATABASE_URL=postgres://user:password@ep-example.eu-central-1.aws.neon.tech/bookmark_sync?sslmode=require

# Client-safe
# Base URL of the sync API, used by the extension and dashboard.
VITE_API_URL=http://localhost:8787
